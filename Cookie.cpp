#include "Cookie.h"
Cookie::Cookie(int y, int x) : Drawing(y, x, 'C')
{
    // Drawing 클래스의 생성자를 호출하여 y, x 좌표와 아이콘 'C'를 설정합니다.
    // 이 부분은 Drawing 클래스의 초기화 리스트에서 이미 수행하고 있습니다.
    // 그러므로 여기서 추가적인 할당이 필요하지 않습니다.
}

